# API base URL for the admin dashboard
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Which shop this dashboard administers. Must match a Tenant.slug row.
# One deployment per customer; the API refuses requests without it.
NEXT_PUBLIC_TENANT_ID=funfsterne
