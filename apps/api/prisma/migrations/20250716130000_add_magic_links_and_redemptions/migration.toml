# Prisma migration metadata
# This migration was generated manually because the local DATABASE_URL in .env is a placeholder.
# Apply it by setting a valid DATABASE_URL/DIRECT_URL and running:
#   npx prisma migrate resolve --applied 20250716130000_add_magic_links_and_redemptions
# or, if the database is empty and reachable:
#   npx prisma migrate deploy
